/**
 * Home — CLAUDE.md §7. Every block is built in Phase 2 from `content/home.ts`.
 */

export const dynamic = 'force-static';

export default function HomePage() {
  return <main className="grow pt-[var(--header-h)]" />;
}
