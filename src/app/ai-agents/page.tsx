/**
 * CLAUDE.md §11.1 — built in a later phase (§13). This stub exists so the
 * navigation resolves and the shell can be verified end to end.
 */

export const dynamic = 'force-static';

export default function AiAgentsPage() {
  return <main className="grow pt-[var(--header-h)]" />;
}
